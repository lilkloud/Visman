# Server
PORT=3001

# Email notifications (optional). If not set, emails are skipped but leads are still stored.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password

# Who receives notification emails. Defaults to SMTP_USER if omitted.
NOTIFY_TO=nnamelekelechi9@gmail.com

# The From header to use when sending emails
MAIL_FROM="Visman <no-reply@visman.local>"
